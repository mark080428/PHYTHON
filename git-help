git init - на делает так, что github прослеживает твои действия (писать все остальные команды только после этой)
Её прописывают только один раз

git remote add origin - связывает проект и репозиторий
Пишут только один раз

Пишутся всегда:
1)git add
2)git commit -m
3)git push